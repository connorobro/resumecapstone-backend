# Server
PORT=5000

# PostgreSQL (AWS RDS)
# Provisioned separately — format: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@host:5432/resume_reviewer

# JWT
# Generate a strong random secret for production (e.g. openssl rand -hex 64)
JWT_SECRET=your_jwt_secret_here

# AWS — pending school approval
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# S3
S3_BUCKET_NAME=your_s3_bucket_name
